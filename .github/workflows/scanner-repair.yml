name: One-time scanner bridge repair
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch scanner bridge
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          p = Path('sistema_inventario.py')
          s = p.read_text(encoding='utf-8')
          pattern = r'''(?ms)^\s{24}const parentDoc = window\.parent\.document;\n\s{24}const inputEl = parentDoc\.querySelector\('input\[placeholder\*="Aguardando bipagem"\]'\);\n\s{24}if \(inputEl\) \{.*?^\s{24}\}'''
          new = '''                        const parentWin = window.parent;
                        const parentDoc = parentWin.document;
                        const inputEl = parentDoc.querySelector('input[placeholder*="Aguardando bipagem"]');
                        if (!inputEl) {
                            document.getElementById('scan-status').innerText = "⚠️ Campo de cadastro não encontrado. Recarregue a página.";
                            return;
                        }
                        try {
                            const setter = Object.getOwnPropertyDescriptor(parentWin.HTMLInputElement.prototype, "value").set;
                            setter.call(inputEl, decodedText);
                            inputEl.dispatchEvent(new parentWin.Event('input', { bubbles: true }));
                            inputEl.dispatchEvent(new parentWin.Event('change', { bubbles: true }));
                            inputEl.focus();
                            document.getElementById('scan-status').innerText = "✅ Lido: " + decodedText + " (enviando...)";
                            setTimeout(() => {
                                const buttons = Array.from(parentDoc.querySelectorAll('button'));
                                const submitBtn = buttons.find(b => (b.innerText || '').includes('Registrar Manualmente'));
                                if (submitBtn) {
                                    submitBtn.click();
                                } else {
                                    document.getElementById('scan-status').innerText = "⚠️ Botão de registro não encontrado. Use o botão manual.";
                                }
                            }, 500);
                        } catch (erro) {
                            document.getElementById('scan-status').innerText = "⚠️ Falha ao transferir o código para o cadastro. Use o botão manual.";
                            console.error('Falha no bridge do scanner:', erro);
                        }'''
          novo, n = re.subn(pattern, new, s, count=1)
          if n != 1:
              raise SystemExit('Bloco do scanner não localizado; nenhuma alteração aplicada.')
          p.write_text(novo, encoding='utf-8')
          PY
          python -m compileall -q sistema_inventario.py
      - name: Commit repair
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sistema_inventario.py
          git commit -m "Corrigir transferência do código do scanner para o cadastro"
          git push
      - name: Remove one-time workflow
        shell: bash
        run: |
          rm -f .github/workflows/scanner-repair.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/workflows/scanner-repair.yml
          git commit -m "Remover workflow temporario de reparo do scanner"
          git push
